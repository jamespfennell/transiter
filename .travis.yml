services:
  - postgresql
language: python
sudo: required
dist: xenial
python: "3.7"
env:
  - TRANSITER_DB_NAME=transiter_test_db TRANSITER_DB_USER=postgres
install:
  - python setup.py install
# command to run tests
script:
  - make integration-tests
  - make unit-tests
after_success:
  - coveralls
